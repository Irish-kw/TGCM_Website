# Table XXV — topic-encoder embedding sensitivity

Run `reproduce.ipynb` to evaluate seven topic-encoder checkpoint families, five seeds, and K=2…6. The displayed statistic is per-sequence-Hungarian Macro-F1 mean and sample standard deviation across seeds.
