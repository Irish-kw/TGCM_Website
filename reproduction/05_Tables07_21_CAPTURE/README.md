# Tables VII and XXI — CAPTure end-to-end evaluation

Run `reproduce.ipynb` to pool single-host and multi-host sequence inputs within each SFM, Zoomer, TREC, and Mix-2…Mix-6 cell. Table VII is the compact Acc/F1 view; Appendix Table XXI is the detailed metric view.

The optional `DOWNLOAD_FULL_CALDERA_CSV` cell downloads and verifies the complete 499-source-CSV provenance snapshot. Standard checkpoint inference uses the sequence-level inputs and does not require extracting the complete raw snapshot.
